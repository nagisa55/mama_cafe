<% if logged_in? %>
  タイムライン
<% else %>
  <div class="container">
    <div class="center jumbotron">
      <h1>Mama Cafe</h1>
      <%= link_to "ログイン", login_path %>
      <p>新規登録は<%= link_to "こちら", signup_path %>から</p>
    </div>
  </div>
<% end %>